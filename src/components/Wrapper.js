import styled from 'styled-components';
import { SCENES_BACKGROUND_COLOR } from '@theme/colors';

const Wrapper = styled.View`
    flex: 1;
    justifyContent: center;
    alignItems: center;
    backgroundColor: ${SCENES_BACKGROUND_COLOR};
`

export default Wrapper;